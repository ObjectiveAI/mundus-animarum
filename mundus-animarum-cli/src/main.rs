fn main() {
    // No commands yet — load and validate the env-driven runtime config so
    // the OBJECTIVEAI_* contract is wired up and enforced from the start.
    let _config = mundus_animarum_cli::run::load_config();
}
